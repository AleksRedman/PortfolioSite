//- Значения задаются в common/vars.pug
//-
//-
ul.socials
  each item in socials
    li.socials__item
      a(href = `${item.link}` target = "_blank" class = `socials__link  socials__link_${item.network}`)= item.network
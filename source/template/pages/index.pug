extends ../_template.pug

block pageTitle
  title Welcome

block content
  .wrapper
    section.container.hero
      .hero__wrapper
        .hero__header
          a.hero__authorization(href = "./authorization.html") Авторизоваться
        .user-card
          include ../sections/_userBox.pug
          +socials('round', socials)
          +nav('user-card-nav', navigation, 2, 3, 'user-card__btn')
        include ../sections/_copyright.pug